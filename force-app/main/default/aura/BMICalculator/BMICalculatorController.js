({
    doInit : function(component, event, helper) {
        helper.loadRecordData(component);
    },
    
    calculateBMI : function(component, event, helper) {
        helper.calculateBMIHelper(component);
    }
})
